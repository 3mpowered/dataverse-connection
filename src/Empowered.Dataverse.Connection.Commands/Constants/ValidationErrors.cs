namespace Empowered.Dataverse.Connection.Commands.Constants;

public static class ValidationErrors
{
    public const string DeviceCodeAndInteractive = "--device-code and --interactive option are mutually exclusive";
    public const string UnknownConnectionType = "Invalid combination of connection arguments";
}